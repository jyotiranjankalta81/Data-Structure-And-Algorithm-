#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{

    ll t;
    cin >> t;
    while (t--)
    {
        ll u, v, x, y;
        cin >> u >> v;
        cout << -(u * u) << " " << (v * v) << endl;
    }

    return 0;
}